from crewai import Task

class SpecManagerTask:
    
    def validation_specification_task(self, agent):
       description = """
       """
       return Task(
            description=description,
            agent=agent,
            expected_output=""
        )  

    def get_specification_task(self, agent):
       description = """
       """
       return Task(
            description=description,
            agent=agent,
            expected_output=""
        )
        
    def update_specification_task(self, agent):
       description = """
       """
       return Task(
            description=description,
            agent=agent,
            expected_output=""
        )

    